apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gpu-jupyter.fullname" . }}-test-connection"
  labels:
    {{- include "gpu-jupyter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gpu-jupyter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
