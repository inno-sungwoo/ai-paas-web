apiVersion: v1
kind: Service
metadata:
  name: {{ include "gpu-jupyter.fullname" . }}
  labels:
    {{- include "gpu-jupyter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gpu-jupyter.selectorLabels" . | nindent 4 }}
